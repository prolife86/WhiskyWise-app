<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/background">

    <!-- Search bar + barcode scan button -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@color/surface">

        <androidx.appcompat.widget.SearchView
            android:id="@+id/searchView"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@color/surface"
            app:queryHint="Search whiskies…"
            app:iconifiedByDefault="false" />

        <ImageButton
            android:id="@+id/btnBarcodeSearch"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:src="@drawable/ic_barcode_scan"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Search by barcode"
            android:layout_marginEnd="4dp" />

    </LinearLayout>

    <!-- Filter row: status spinner | sort spinner | retired checkbox -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@color/surface"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:paddingTop="4dp"
        android:paddingBottom="4dp">

        <!-- Status spinner -->
        <Spinner
            android:id="@+id/spinnerStatus"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@color/surface"
            android:paddingStart="4dp"
            android:paddingEnd="4dp"
            android:entries="@array/status_labels"
            android:dropDownWidth="match_parent" />

        <View
            android:layout_width="1dp"
            android:layout_height="24dp"
            android:background="@color/divider"
            android:layout_marginStart="4dp"
            android:layout_marginEnd="4dp" />

        <!-- Sort spinner -->
        <Spinner
            android:id="@+id/spinnerSort"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@color/surface"
            android:paddingStart="4dp"
            android:paddingEnd="4dp"
            android:entries="@array/sort_labels"
            android:dropDownWidth="match_parent" />

        <View
            android:layout_width="1dp"
            android:layout_height="24dp"
            android:background="@color/divider"
            android:layout_marginStart="4dp"
            android:layout_marginEnd="4dp" />

        <!-- Retired checkbox -->
        <CheckBox
            android:id="@+id/checkRetired"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Inc. retired"
            android:textSize="12sp"
            android:textColor="@color/on_surface_dim" />

    </LinearLayout>

    <!-- Second filter row: flavour | min score | max price -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@color/surface"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:paddingBottom="6dp">

        <Spinner
            android:id="@+id/spinnerFlavor"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="2"
            android:background="@color/surface"
            android:paddingStart="4dp"
            android:paddingEnd="4dp"
            android:entries="@array/flavor_filter_labels"
            android:dropDownWidth="match_parent" />

        <View
            android:layout_width="1dp"
            android:layout_height="24dp"
            android:background="@color/divider"
            android:layout_marginStart="4dp"
            android:layout_marginEnd="4dp" />

        <EditText
            android:id="@+id/etMinScore"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:inputType="numberDecimal"
            android:hint="Min score"
            android:textSize="12sp"
            android:textColor="@color/on_surface"
            android:textColorHint="@color/on_surface_dim"
            android:background="@color/surface"
            android:paddingStart="4dp"
            android:paddingEnd="4dp"
            android:maxLines="1" />

        <View
            android:layout_width="1dp"
            android:layout_height="24dp"
            android:background="@color/divider"
            android:layout_marginStart="4dp"
            android:layout_marginEnd="4dp" />

        <EditText
            android:id="@+id/etMaxPrice"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:inputType="numberDecimal"
            android:hint="Max €"
            android:textSize="12sp"
            android:textColor="@color/on_surface"
            android:textColorHint="@color/on_surface_dim"
            android:background="@color/surface"
            android:paddingStart="4dp"
            android:paddingEnd="4dp"
            android:maxLines="1" />

    </LinearLayout>

    <TextView
        android:id="@+id/errorBanner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp"
        android:background="#FFE0E0"
        android:textColor="#C00000"
        android:visibility="gone" />

    <!-- FrameLayout lets the FAB float over the list at bottom-end -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
            android:id="@+id/swipeRefresh"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent">

                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/recyclerView"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:clipToPadding="false"
                    android:paddingBottom="80dp"
                    app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager" />

                <TextView
                    android:id="@+id/emptyState"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center"
                    android:text="No whiskies yet.\nTap + to add one."
                    android:textAlignment="center"
                    android:textColor="@color/on_surface_dim"
                    android:visibility="gone" />
            </FrameLayout>
        </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

        <!-- Loading-more footer: visible while fetching the next page -->
        <ProgressBar
            android:id="@+id/loadingMoreBar"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="4dp"
            android:layout_gravity="bottom"
            android:layout_marginBottom="80dp"
            android:indeterminate="true"
            android:indeterminateTint="@color/amber"
            android:visibility="gone" />

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fab"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_margin="16dp"
            android:src="@android:drawable/ic_input_add"
            app:backgroundTint="@color/amber"
            android:contentDescription="Add whisky" />

    </FrameLayout>

</LinearLayout>
